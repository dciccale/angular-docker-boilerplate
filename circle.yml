machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install docker-compose

test:
  override:
    - docker-compose run -d --no-deps web
    - gulp test

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
